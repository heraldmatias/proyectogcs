# -*- coding: utf-8 -*-
from django.conf.urls.defaults import patterns, url

urlpatterns = patterns('home.views',
    url(r'^$','index', name='ogcs-index'),
    url(r'^login/$','login', name='ogcs-login'),    
)
